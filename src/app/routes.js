import Home from "components/Home";
import Counter from "components/Counter";

const routes = {
  home: {
    path: "/",
    component: Home,
  },
  counter: {
    path: "/counter",
    component: Counter,
  },
};

export default routes;
