import { defineMessages } from "react-intl";
const scope = "features.homePage.";

export default defineMessages({
  checkOutCounter: {
    id: `${scope}checkOutCounter`,
    defaultMessage: "Check out counter implemented with redux-saga <a>here</a>.",
  },
  learnReact: {
    id: `${scope}learnReact`,
    defaultMessage: "Learn React",
  },
});
